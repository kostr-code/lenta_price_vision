services:
  postgres:
    ports: !reset []

  backend:
    environment:
      APP_ENV: ci
      LOG_LEVEL: WARNING
      AUTO_CREATE_TABLES: "1"
      DATABASE_URL: postgresql+psycopg://price_audit:price_audit@postgres:5432/price_audit
      ML_URL: http://host.docker.internal:8000
